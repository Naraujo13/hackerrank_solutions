
        
        HashSet<javafx.util.Pair> set = new HashSet<>();
        for (int i = 0; i < t; i++) {
            set.add(new javafx.util.Pair(pair_left[i], pair_right[i]));
            System.out.println(set.size());
        }


